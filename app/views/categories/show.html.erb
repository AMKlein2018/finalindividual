<h1><%= @category.title %></h1>




<% @category.blogs.each do |b|%>
    <%= b.title %><br />
    <%= b.content %>
   	<% b.comments.each do |b| %>
		<%= b.content %>
	<% end %>
	<p>
		<strong>Image:</strong>
		<img src="<%= b.image.url()%>">
	</p>
<% end %>